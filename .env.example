# Klang FusionAuth Configuration
# This is already hardcoded to use Klang's FusionAuth instance at https://login.seed.game
# But you can override it if needed for testing
# NEXT_PUBLIC_FUSIONAUTH_URL=https://login.seed.game

# OAuth Client ID is already configured in authConfig.ts
# KLANG_OAUTH_CLIENT_ID=b17dec48-2a07-4c12-9cda-8778d9209707

# Klang FusionAuth Configuration
# This is already hardcoded to use Klang's FusionAuth instance at https://login.seed.game
# But you can override it if needed for testing
# NEXT_PUBLIC_FUSIONAUTH_URL=https://login.seed.game

# OAuth Client ID is already configured in authConfig.ts
# KLANG_OAUTH_CLIENT_ID=b17dec48-2a07-4c12-9cda-8778d9209707

# Database Configuration (Vercel Postgres)
DATABASE_URL="postgres://username:password@host:port/database?sslmode=require"