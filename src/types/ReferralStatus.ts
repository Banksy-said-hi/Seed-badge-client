export enum ReferralStatus {
  // Loading referral code status
  Loading = "Loading",
  // No referral code applied
  None = "None",
  // A referral code has already been applied
  Completed = "Completed",
}
